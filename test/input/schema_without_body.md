# Some resource [POST /some/resource]

+ Request

    + Schema

        {
            "type": "object",
            "properties": {
                "prop1": {
                    "type": "string"
                }
            },
            "required": ["prop1"]
        }

+ Response 200 (text/plain)
